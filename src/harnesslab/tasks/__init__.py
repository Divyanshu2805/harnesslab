"""Task definitions.

A task definition holds **no generated content** -- only a generator call. That
is what makes the suite contamination-resistant: two seeds produce genuinely
different instances, and no instance existed to be memorised.

Day 1 ships only `hello`, which exists to prove the framework is wired up end to
end. The real families arrive with SPEC-005 and SPEC-012 through 016.
"""
